== Welcome to GreenBooking

* Rails version:'4.1.8'

* Ruby version: 2.15

In the project folder run:

bundle Install
rake db:setup
rake spec
rake test

* Gem initialization:      bundle Install


* Database Creation, seeding and initialization : rake db:setup

* Runing the test suite:
rake spec
rake test

documentation: rake doc:app


